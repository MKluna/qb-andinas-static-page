---
import { CONSULTIN_TXT } from "../../../mock/public-txt";
import Title from "../../shared/Title.astro";
import CardItemsConsultin from "../../cards/CardItemsConsultin.astro";

const IMPORTAR_ESTO_DESPUES = {
    SEGURIDAD_Y_SALUD:
        "La seguridad laboral es crucial en todo ámbito. Incluye prevención de incendios, riesgos eléctricos y medidas preventivas. Se enseña sobre trabajo en alturas, normativas y protección, asegurando ambientes seguros y minimizando riesgos.",
    SISTEMA_DE_GESTION:
        "Nos enfocamos en capacitación y asesoramiento especializado. Implementamos sistemas de gestión ISO 9001, ISO 14001 e ISO 45001 para calidad, ambiental y salud ocupacional. Realizamos auditorías internas para cumplimiento y mejora continua, asegurando estándares internacionales.",
    DESARROLLO_HUMANO:
        "Invertir en el desarrollo humano es esencial para el éxito sostenible de las empresas y el bienestar de sus empleados. Fomenta la autoconciencia, la autoestima y el crecimiento individual, beneficiando tanto a los empleados como a la empresa.",
    CONTROL_DE_CALIDAD:
        "Nos centramos en la eficiencia operativa. Especializados en elaborar instructivos claros y procedimientos detallados. Detectamos anomalías, analizamos causas y aplicamos acciones correctivas. Utilizamos diagramas de correlación y gravedad para gestión precisa de problemas.",
};

import control_de_calidad_Image from "../../../assets/consultoria/control_de_calidad.png";
import desarrollo_humano_Image from "../../../assets/consultoria/desarrollo_humano.png";
import seguridad_y_salud_Image from "../../../assets/consultoria/seguridad_y_salud.png";
import sistema_de_gestion_Image from "../../../assets/consultoria/sistema_de_gestion.png";

const CARDS_DETAILS = [
    {url: control_de_calidad_Image, text: [IMPORTAR_ESTO_DESPUES.CONTROL_DE_CALIDAD]},
    {url: desarrollo_humano_Image, text: [IMPORTAR_ESTO_DESPUES.DESARROLLO_HUMANO]},
    {url: seguridad_y_salud_Image, text: [IMPORTAR_ESTO_DESPUES.SEGURIDAD_Y_SALUD]},
    {url: sistema_de_gestion_Image, text: [IMPORTAR_ESTO_DESPUES.SISTEMA_DE_GESTION]},
]

---

<section id="consulting">
    <div class="w-full relative py-8 md:py-10 px-6 md:px-8 rounded-2xl">
        <div class="relative">
            <div class="relative mb-2 mt-12">
                <div class="text-center max-w-3xl mx-auto mb-2">
                    <Title>{CONSULTIN_TXT.TITLE}</Title>
                </div>
                <div
                    class="w-full relative py-8 md:py-10 px-6 md:px-8 rounded-2xl"
                >
                    <div
                        class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-12"
                    >
                        {
                            CARDS_DETAILS.map((service, index) => (
                                <CardItemsConsultin
                                    url={service.url}
                                    alt={`cards-details-${index}`}
                                    text={service.text}
                                    key={index}
                                />
                            ))
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
        const observer = new IntersectionObserver((entries, observer) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    entry.target.classList.add(
                        "animate__animated",
                        "animate__fadeInUp",
                        "animate__slower",
                    );
                    observer.unobserve(entry.target);
                }
            });
        });

        document
            .querySelectorAll(".animate-on-scroll-consulting")
            .forEach((element, index) => {
                element.style.animationDelay = `${index * 0.1}s`;
                observer.observe(element);
            });
    });
</script>
